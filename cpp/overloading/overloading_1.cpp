#include<iostream>

class Cents {
    private:
        int m_cents;
    public:
        Cents(m_cents) {
            m_cents = m_cents;
        }
        friend Cents operator + (Cents &s, int value);
        friend Cents operator 
